[package]
name = "comparand-corpus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Golden-case schema and validation corpus for the Comparand sequence-comparison toolkit."
categories = ["algorithms", "development-tools::testing"]
keywords = ["comparison", "validation", "golden", "corpus", "testing"]

[features]
default = ["std"]
std = ["alloc", "comparand-core/std"]
alloc = ["comparand-core/alloc"]

[dependencies]
comparand-core = { workspace = true }

[lints]
workspace = true
